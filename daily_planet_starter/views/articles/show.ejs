<h1><%= article.title %></h1>

<div class="well">
  <p><%= article.body %></p>
</div>

<button class="Edit">Edit</button>
<!-- this should show the form -->


<form action="/article/:id" class="put-form">
  <label for="body">New Body</label>
  <input type="text" name="body" value="<%= article.body %>">
  <input type="submit" value="Update">
</form>


<!-- This is not working  -->
<script>
  $('.put-form').on('submit', function(e) {
    e.preventDefault();
    var updateElement = $(this);
    var updateUrl = updateElement.attr('action');
    var updateData = updateElement.find('[name="body"]')[0].value;
    console.log(updateData);
    $.ajax({
    method: 'PUT',
    url: updateUrl,
    data: updateData
    }).done(function(data) {
    console.log(data);
});
</script>
